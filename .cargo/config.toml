[build]
target = "i686-king.json"

[unstable]
# Add "alloc" to the list below
build-std = ["core", "compiler_builtins", "alloc"]
build-std-features = ["compiler-builtins-mem"]

[target.'targetjson(i686-king.json)']
rustflags = [
    "-C", "code-model=small",
    "-C", "relocation-model=static",
]